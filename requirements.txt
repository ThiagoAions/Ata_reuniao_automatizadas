# Ata Facial API — Dependências (Engenharia de Guerrilha)
# ═══════════════════════════════════════════════════════════
# Apenas OpenCV (sem YOLO ~800MB, sem dlib ~400MB)
# Container final: ~200MB | RAM: ~80MB

# Framework Web
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Motor de IA (face_recognition + OpenCV fallback)
face-recognition>=1.3.0
opencv-contrib-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Configuração
python-dotenv>=1.0.0

# Validação de dados
pydantic>=2.0.0
